# ─── Core dependencies ────────────────────────────────────────────────
lark>=1.0,<2.0                   # LALR parser for grammar enforcement
numpy>=1.20,<3.0                 # Counterfactual generation (Dirichlet sampling)
scikit-learn>=1.0,<2.0           # Decision tree / random forest rule extraction
pandas>=1.3,<3.0                 # Data loading utilities
openpyxl>=3.0,<4.0               # Excel file reading

# ─── Testing ──────────────────────────────────────────────────────────
pytest>=7.0,<9.0                 # Test framework
pytest-cov>=4.0,<8.0             # Coverage reporting

# ─── Configuration ────────────────────────────────────────────────────
pyyaml>=6.0,<7.0                 # YAML config file loading

# ─── Logging & Reporting ──────────────────────────────────────────────
colorama>=0.4,<1.0               # Colored terminal output (Windows compat)
